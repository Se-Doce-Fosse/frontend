/* import { getAllProducts } from '../view-controllers/ProductsDashboard.view.controller'
import { useUser } from '../../context/UserContext'

const { user } = useUser();
export const parseProducts =  () => {
    try{
        const data = getAllProducts(user.token);
        console.log(data);

    }
} */
